interface:
  display_name: "GitHub PR Create"
  short_description: "Create GitHub pull requests from the current branch."
  default_prompt: "Create a pull request for the current branch using the repository policy."
